EchoRecovery secrets (local only — do not share or commit)

# Required
RPC_URL=https://eth.llamarpc.com
CHAIN_ID=1
USDT_ADDRESS=0xdAC17F958D2ee523a2206206994597C13D831ec7
USDT_DECIMALS=6
CONTRACT_ADDRESS=PASTE_AFTER_DEPLOY

# Private key is stored separately in pk.txt as 0x + 64 hex.
# If you insist on keeping it here, add: PRIVATE_KEY=0xYOUR_PRIVATE_KEY

# Quick loaders (PowerShell — paste one line at a time in the project folder)
# Set RPC + chain + token
$env:RPC_URL="https://eth.llamarpc.com"; $env:CHAIN_ID="1"; $env:USDT_ADDRESS="0xdAC17F958D2ee523a2206206994597C13D831ec7"; $env:USDT_DECIMALS="6"
# After deployment, set your contract address
$env:CONTRACT_ADDRESS="0xPASTE_DEPLOYED_ADDRESS"
# Load private key from pk.txt
$env:PRIVATE_KEY=(Get-Content -Raw .\pk.txt).Trim()

# Deploy
python .\deploy_contract.py

# Automated direct USDT transfer (recommended)
# 1) Put your private key in pk.txt (0x + 64 hex)
# 2) Send a small test first (1 USDT):
#    .\send_usdt.ps1 -Recipient "0xPASTE_WALLET" -AmountUSDT 1.00
# 3) Then send the full amount similarly

# Legacy manual (advanced; may fail if misconfigured) — not recommended
# $env:RECIPIENT="0xPASTE_WALLET"; $env:AMOUNT_USDT="10.00"; $env:CONTRACT_ABI_PATH="contract-abi.json"; python .\send.py